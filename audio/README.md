# 《游京》背景音乐

请把你有权在本网站使用的浏览器标准音频放在本目录，并使用下列任一文件名：

- `youjing.m4a`（推荐，AAC 编码）
- `youjing.mp3`

页面会依次尝试这两个文件。若都不存在或浏览器无法播放，右上角按钮会自动切换为“古琴”，继续使用站内合成音乐。

网易云音乐下载的 `.ncm` 是客户端专用封装，HTML `<audio>` 不能直接播放；重命名扩展名也不会变成 M4A 或 MP3。
